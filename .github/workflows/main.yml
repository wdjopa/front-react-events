
name: Node.js CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [10.x, 12.x, 14.x]

    steps:
    - uses: actions/checkout@v2
    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v1
      with:
        node-version: ${{ matrix.node-version }}
    - run: npm ci
    - run: npm run build --if-present
    - run: npm test
    - run: cd build
    - run: zip -r kata_events.zip .
    - run: mv kata_events.zip ../kata_events.zip
    - |
      curl -v -X POST -H "Accept: application/json" -H "Authorization: Bearer 43|QLhuks7hi0NUgF4gzT70lTfEUvQeirA4EuTIqMjO" -F "file=@./kata_events.zip" https://hostme.space/api/websites/kata-events/deploy_on_push
